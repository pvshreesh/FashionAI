# Fashion AI Agent Notes

Use `ponytail full` by default in this repository.

Guidelines:
- Prefer the smallest working diff after reading the full code path.
- Reuse existing helpers and patterns before adding new code.
- Prefer stdlib, platform features, and already-installed dependencies over new packages.
- Avoid speculative abstractions, scaffolding, and config that only exists for "later".
- Keep explanations short unless the user asks for depth.

Stop only if the user explicitly says `stop ponytail` or asks for a fuller build-out.
